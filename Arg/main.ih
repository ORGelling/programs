#include <arg/arg.h>
#include <iostream>
#include <fstream>

using namespace std;    // 
                                // Helper functions for test program
int usage(int error);
int countlines(fstream &file);
int read(string const &filename);
int writeremove(Arg &arg, fstream &file, string *args);
void stats(Arg &arg, string *args);
void process
(
    Arg &arg, 
    fstream &file, 
    string *args, 
    int const add, 
    int const clip
);
